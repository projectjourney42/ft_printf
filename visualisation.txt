# --- library ---

	#include <strarg.h> #pour utiliser va_list

# --- ft_printf ---
	return (int);
	%% pour afficher "%"

# --- Context ---

printf ("blabla : %- bla", ...)
ft_printf(char	*format	,variadic)
	va_list print;
        va_start(print,format);
	while (format[i])

	|if ( format[i] == %)
	|	(format[i + 1]) ==
	|			- d : int, decimal
	|				>int de = va_arg(print, int);
	|				>//char dec = ft_itoa(de); =>ft_putstr(dec); 		
	|			- c : char
	|				>ft_putchar(va_arg(print, char));
	|			- s : char *
	|				>ft_putstr(va_arg(print, char *));
	|		- x : lower hexadecimal
	|		- p : (void *) @pointer en hexa
	|		- i : int in base 10
	|			> meme que d
	|		- u : unsigned decimal
	|			> // nbr > 0
	|			> meme que d
	|		- X : uppercase hexa
	|		- % : print "%"
	|			>ft_putchar('%')
	|			++i;
	|		else
	|			>ft_puchar(format[i]);
	|			>ft_putchar(format[i + 1]);
	else
		ft_puchar(format[i]);
	i++;
	va_end(print);

# --- Files ---
	__ft_printf_number.c ==> %d, %i --> ft_putnnr_fd(..., 1) | ft_itoa(...)
	__ft_printf_unsigned_nbr.c ==> %u (nbr  > 0)
	__ft_printf_char.c ==> %c, %s --> ft_puchar_fd(..., 1) & ft_putstr_fd(..., 1);
	__ft_printf_hexa.c ==> %x, %X (nbr > 0) -->ft_putchar_fd(..., 1)
	__ft_printf_address.c ==> %p
	__Makefile ==> make the libft
	__ft_printf.h ==> #include "libft.h"
	__libft
		__ft_*.c
	
